<label for="{{ field.id }}" class="min-w-0 block">
  <span class="block text-sm font-medium text-gray-700 mb-1">{{ field.label }}{% if field.required %} *{% endif %}</span>
  <input id="{{ field.id }}" type="color" name="{{ field.inputName }}"
         value="{{ field.value }}"
         class="block h-10 min-w-0 w-full max-w-full rounded-lg border border-gray-300"
         {% if field.required %}required{% endif %}>
</label>
