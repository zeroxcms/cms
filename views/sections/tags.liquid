<div class="px-4 py-5 sm:px-6 sm:py-8 lg:px-8">
  <div class="flex flex-col gap-4 mb-6 sm:flex-row sm:items-center sm:justify-between">
    <h2 class="text-2xl font-bold text-gray-900">Tags</h2>
    <div class="flex flex-row gap-2 sm:items-center">
      <a href="/admin/taxonomies"
         title="Taxonomies"
         aria-label="Taxonomies"
         class="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-gray-300 bg-white px-4 text-sm font-semibold text-gray-700 hover:bg-gray-50">
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><use href="/assets/icons.svg#tag"></use></svg>
        <span class="hidden lg:inline">Taxonomies</span>
      </a>
      <a href="/admin/tags/new"
         title="New Tag"
         aria-label="New Tag"
         class="inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-indigo-600 px-4 text-sm font-semibold text-white hover:bg-indigo-700">
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><use href="/assets/icons.svg#plus"></use></svg>
        <span class="hidden lg:inline">New Tag</span>
      </a>
    </div>
  </div>
  <form method="GET" class="mb-4 flex min-w-0 flex-col gap-2 sm:flex-row sm:flex-wrap sm:items-center">
    <label for="filter_taxonomy" class="sr-only">Filter by taxonomy</label>
    <select id="filter_taxonomy" name="filter_taxonomy" class="min-w-0 w-full px-3 py-2 border border-gray-300 rounded-lg text-sm sm:w-auto">
      <option value="0">All taxonomies</option>
      {% for option in filterOptions %}
        <option value="{{ option.id }}" {% if option.selected %}selected{% endif %}>{{ option.name | escape }}</option>
      {% endfor %}
    </select>
    <button type="submit"
            title="Filter"
            aria-label="Filter tags"
            class="inline-flex h-10 w-10 shrink-0 items-center justify-center rounded-lg border border-gray-300 bg-white text-gray-700 hover:bg-gray-50">
      <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><use href="/assets/icons.svg#list-filter"></use></svg>
    </button>
  </form>
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-x-auto">
    <table class="w-full min-w-[560px] text-left">
      <thead class="bg-gray-50 border-b border-gray-200">
        <tr>
          <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Name</th>
          <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Slug</th>
          <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Type</th>
          <th class="px-6 py-3"></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        {% if hasTags %}
          {% for tag in tags %}
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-3 text-sm font-medium text-gray-900">{{ tag.name | escape }}</td>
              <td class="px-6 py-3 text-sm font-mono text-gray-500">{{ tag.slug | escape }}</td>
              <td class="px-6 py-3 text-sm text-gray-500">{{ tag.taxonomyName | escape }}</td>
              <td class="px-6 py-3 text-right">
                <a href="{{ tag.editHref | escape }}" class="text-sm font-medium text-indigo-600 hover:text-indigo-800">Edit</a>
              </td>
            </tr>
          {% endfor %}
        {% else %}
          <tr><td colspan="4" class="px-6 py-10 text-center text-gray-400">No tags yet.</td></tr>
        {% endif %}
      </tbody>
    </table>
  </div>
</div>
