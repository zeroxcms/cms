name = "cms-plugin-events"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# After registering this Worker's URL in Admin -> Plugins, copy that
# registration's dedicated secret here:
#   wrangler secret put PLUGIN_SECRET
